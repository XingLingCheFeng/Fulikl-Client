package missu.epsilon.client.utils.entity;

/**
 * Marker enum for identifying the source of buffered packets.
 * Extend with additional entries as needed by modules.
 */
public enum DelayModules {
    NONE,
    GENERIC
}
